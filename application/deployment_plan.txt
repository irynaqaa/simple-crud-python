1. Setting up the production environment: 
    - Install and configure the web server, database, and other necessary software.
    - Configure the server for security, performance, and scalability.
2. Deploying the code: 
    - Use a version control system to manage and deploy the code.
    - Automate the deployment process using scripts or tools.
3. Configuring the system: 
    - Configure the system settings, such as database connections, API keys, and other environment-specific settings.
    - Set up logging, monitoring, and alerting to ensure the system is running smoothly.
4. Ensuring automation and repeatability: 
    - Use automation tools, such as Ansible or Jenkins, to automate the deployment process.
    - Use infrastructure-as-code tools, such as Terraform or CloudFormation, to manage the infrastructure.
5. Testing and validation: 
    - Test the system thoroughly after deployment to ensure it is working as expected.
    - Validate the system configuration and settings to ensure they are correct.
6. Rollback and recovery: 
    - Have a plan in place for rolling back to a previous version in case of issues.
    - Have a plan in place for recovering from failures or disasters.

The provided command 'cd application/' is a shell command and not a code block that can be executed. It is used to change the current directory to 'application/'. This command should be executed in a terminal or command prompt, not in a code editor or IDE.