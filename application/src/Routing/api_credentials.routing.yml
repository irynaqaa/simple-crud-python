routes:
  api_credentials:
    path: '/api/credentials'
    defaults:
      _controller: '\Application\src\Controller\ApiCredentialsController::build'
      _title: 'API Credentials'
    requirements:
      _permission: 'access content'}